
name: ci angular pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main, feature1, hotfix]

jobs:
  build:
    runs-on: ubuntu-latest
  
    steps:
      - name: to checkout code
        uses: actions/checkout@v5
